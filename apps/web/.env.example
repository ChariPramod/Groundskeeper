# Default mode needs no services or credentials.
DASHBOARD_MODE=demo
# For live mode, use this file as apps/web/.env.local and fill all three values.
# Do not prefix the access token or database URL with NEXT_PUBLIC_.
DASHBOARD_INSTALLATION_ID=
DASHBOARD_ACCESS_TOKEN=
DATABASE_URL=
