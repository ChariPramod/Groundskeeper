DATABASE_URL=postgresql://groundskeeper:groundskeeper@localhost:5432/groundskeeper
APP_ID=
PRIVATE_KEY=
# Alternatively point to the downloaded PEM file (relative to workspace root or absolute).
# PRIVATE_KEY takes precedence; leave it blank to use the path.
PRIVATE_KEY_PATH=
# PRIVATE_KEY also accepts quoted multiline PEM, literal \n escapes, or base64 PEM.
WEBHOOK_SECRET=
# Optional local webhook relay, configured in your GitHub App settings.
WEBHOOK_PROXY_URL=
# Use a dedicated test database. Explicit integration tests write isolated test records.
DATABASE_TEST_URL=
